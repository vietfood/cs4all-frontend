---
import Link from '@/components/Link.astro'
import PageHead from '@/components/PageHead.astro'
import { Button } from '@/components/ui/button'
import Layout from '@/layouts/Layout.astro'
import { getCollection } from 'astro:content'

const subjects = await getCollection('subject')
subjects.sort((a, b) => (a.data.order ?? 999) - (b.data.order ?? 999))
---

<Layout class="max-w-3xl">
  <PageHead slot="head" title="Home" />

  <section class="mt-12 mb-24 flex flex-col items-center text-center sm:mt-20">
    <div
      class="focus:ring-ring bg-secondary text-secondary-foreground hover:bg-secondary/80 mb-6 inline-flex items-center rounded-full border border-transparent px-2.5 py-0.5 text-xs font-semibold transition-colors focus:ring-2 focus:ring-offset-2 focus:outline-none"
    >
      Chào mừng đến với cs4all-vn 👋
    </div>
    <h1
      class="mb-6 text-4xl font-extrabold tracking-tight sm:text-5xl md:text-6xl"
    >
      <span
        class="text-primary from-primary bg-gradient-to-r to-blue-600 bg-clip-text text-transparent"
        >Khoa học Máy tính</span
      >
      <br /> cho mọi nhà 📚
    </h1>
    <p class="text-muted-foreground mb-10 max-w-2xl text-lg leading-relaxed">
      Nơi chia sẻ kiến thức nền tảng và chuyên sâu về CS - từ tài liệu, bí kíp
      về Machine Learning, Reinforcement Learning cho đến các chủ đề "khó nhằn"
      khác.
    </p>
    <div class="flex flex-col gap-4 sm:flex-row">
      <Link href="#subjects" class="w-full sm:w-auto">
        <Button
          size="lg"
          className="h-12 w-full rounded-full px-8 font-semibold"
          >Bắt đầu ngay</Button
        >
      </Link>
      <Link href="/about" class="w-full sm:w-auto">
        <Button
          variant="outline"
          size="lg"
          className="h-12 w-full rounded-full px-8">Tìm hiểu thêm</Button
        >
      </Link>
    </div>
  </section>

  <section id="subjects" class="mb-16 scroll-mt-24">
    <div class="mb-8 flex flex-col gap-2">
      <h2 class="text-2xl font-bold tracking-tight sm:text-3xl">Các chủ đề</h2>
      <p class="text-muted-foreground">
        Chọn một chủ đề để bắt đầu lộ trình học tập của bạn.
      </p>
    </div>
    <div class="grid grid-cols-1 gap-6 md:grid-cols-2">
      {
        subjects.map((subject) => (
          <Link href={`/${subject.id}`} class="group block h-full">
            <div class="bg-card text-card-foreground hover:border-primary/50 relative h-full overflow-hidden rounded-2xl border shadow-sm transition-all duration-300 hover:-translate-y-1 hover:shadow-lg">
              <div class="from-primary/5 absolute inset-0 bg-gradient-to-br via-transparent to-transparent opacity-0 transition-opacity group-hover:opacity-100" />
              <div class="relative z-10 flex h-full flex-col p-6 sm:p-8">
                <div class="bg-primary/10 text-primary mb-4 inline-flex h-12 w-12 items-center justify-center rounded-lg">
                  <svg
                    xmlns="http://www.w3.org/2000/svg"
                    width="24"
                    height="24"
                    viewBox="0 0 24 24"
                    fill="none"
                    stroke="currentColor"
                    stroke-width="2"
                    stroke-linecap="round"
                    stroke-linejoin="round"
                    class="lucide lucide-book-open-text"
                  >
                    <>
                      <path d="M2 3h6a4 4 0 0 1 4 4v14a3 3 0 0 0-3-3H2z" />
                      <path d="M22 3h-6a4 4 0 0 0-4 4v14a3 3 0 0 1 3-3h7z" />
                      <path d="M6 8h2" />
                      <path d="M6 12h2" />
                      <path d="M16 8h2" />
                      <path d="M16 12h2" />
                    </>
                  </svg>
                </div>
                <h3 class="group-hover:text-primary mb-3 text-xl font-bold transition-colors">
                  {subject.data.title}
                </h3>
                <p class="text-muted-foreground flex-grow leading-relaxed">
                  {subject.data.description}
                </p>
                <div class="text-primary mt-6 flex items-center text-sm font-medium">
                  Bắt đầu học
                  <svg
                    xmlns="http://www.w3.org/2000/svg"
                    width="24"
                    height="24"
                    viewBox="0 0 24 24"
                    fill="none"
                    stroke="currentColor"
                    stroke-width="2"
                    stroke-linecap="round"
                    stroke-linejoin="round"
                    class="ml-1 size-4 transition-transform group-hover:translate-x-1"
                  >
                    <>
                      <path d="M5 12h14" />
                      <path d="m12 5 7 7-7 7" />
                    </>
                  </svg>
                </div>
              </div>
            </div>
          </Link>
        ))
      }
    </div>
  </section>
</Layout>
